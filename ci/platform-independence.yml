# Copyright (c) 2021 Digital Asset (Switzerland) GmbH and/or its affiliates. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

steps:
  - bash: ci/dev-env-install.sh
    displayName: 'Build/Install the Developer Environment'
  - bash: ci/configure-bazel.sh
    displayName: 'Configure Bazel for root workspace'
    env:
      IS_FORK: $(System.PullRequest.IsFork)
      # to upload to the bazel cache
      GOOGLE_APPLICATION_CREDENTIALS_CONTENT: $(GOOGLE_APPLICATION_CREDENTIALS_CONTENT)
  - task: DownloadPipelineArtifact@0
    inputs:
      artifactName: 'platform-independence-dalf'
      targetPath: $(Build.StagingDirectory)/
  - bash: |
      set -euo pipefail
      eval "$(dev-env/bin/dade-assist)"
      bazel build //compiler/damlc/tests:platform-independence.dalf
      diff bazel-bin/compiler/damlc/tests/platform-independence.dalf $(Build.StagingDirectory)/platform-independence.dalf
    displayName: 'Rebuild platform-independence.dalf and compare Linux/Windows dalfs'
